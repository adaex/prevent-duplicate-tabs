{
  "scripts": {
    "dev": "npm run init && tsc -w",
    "build": "npm run init && tsc",
    "init": "mkdir -p dist && rm -rf dist/*.* && cp -r manifest.json icon/* dist"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.242",
    "typescript": "^5.1.6"
  }
}
